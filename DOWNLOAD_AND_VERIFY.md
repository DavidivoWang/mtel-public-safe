# Download and verification

Use an immutable release artifact together with its external `ARTIFACT_BINDING.json` and SHA-256 sidecar. The binding sidecar records the repository commit, public-file inventory hash, delivery ZIP hash, and exact wheel hash.

Do not treat a moving branch archive as a released artifact.
